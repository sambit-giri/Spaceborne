# This is the environment file for the `spaceborne` conda environment to be used on Linux machines

name: spaceborne
channels:
  - defaults
  - conda-forge
dependencies:
  - python>=3.12
  - matplotlib
  - numpy
  - scipy
  - pandas
  - tqdm
  - pip
  - pyyaml
  - astropy
  - joblib
  - numba
  - camb
  - gcc
  - healpy
  - pyqt
  - ipympl
  - cfitsio

  # for CCL:
  - fftw
  - gsl
  - swig
  - cmake
  - make
  - libgcc-ng
  - libgfortran-ng
  - libstdcxx-ng
  - fast-pt
  # - cfitsio  # not necessary?
  - pip:
    - pymaster
    - cython
    - getdist

# After installing the environment, run
# pip install git+https://github.com/davidesciotti/CCL.git@master
# to install the CCL fork used in this project

# In case of issues with this last step, try
# $ sudo apt-get install gfortran cmake build-essential autoconf bison
# and then retry the pip install command above
